// app.js — ENTRY POINT
//
// Boots the app once the DOM is ready and wires the other layers
// together (data -> state -> virtualList -> render, plus the keyboard
// and compose event handlers). Owns no logic itself — if you're looking
// for "how something works," it's never here, only "how it's connected."
//
// Built starting Phase 2.
